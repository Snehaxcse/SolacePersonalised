[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Anthropic calls often exceed Netlify's 10s default.
[functions.claude]
  timeout = 26

[dev]
  command = "npm run dev"
  targetPort = 5173
  framework = "vite"
